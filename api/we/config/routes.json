{
  "routes": [
    {
      "method": "GET",
      "path": "/we",
      "handler": "we.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/we/count",
      "handler": "we.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/we/:id",
      "handler": "we.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/we",
      "handler": "we.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/we/:id",
      "handler": "we.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/we/:id",
      "handler": "we.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
